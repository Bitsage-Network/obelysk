# Obelysk Dark Pool Relay Service
# Copy to .env and fill in required values

# ── Server ──────────────────────────────────────────────────────────────────
PORT=3001

# ── Starknet ────────────────────────────────────────────────────────────────
# RPC endpoint for transaction submission and status checks
RPC_URL=https://starknet-sepolia.g.alchemy.com/starknet/version/rpc/v0_7/demo
# Required: Private key of the funded relayer account
RELAY_PRIVATE_KEY=0x...
# Required: Address of the funded relayer account
RELAY_ACCOUNT_ADDRESS=0x...
# Dark Pool contract address
DARK_POOL_ADDRESS=0x03534599fbdfc28e12148560363fbe2551a6dfdea9901a9189f27e1f22b4ef94

# ── Authentication ──────────────────────────────────────────────────────────
# Required in production: API key for relay endpoint
RELAY_API_KEY=

# ── Rate Limiting ───────────────────────────────────────────────────────────
# Max requests per owner per window (default: 20)
RATE_LIMIT_MAX=20
# Rate limit window in milliseconds (default: 300000 = 5 min)
RATE_LIMIT_WINDOW_MS=300000

# ── Redis (optional) ────────────────────────────────────────────────────────
# Falls back to in-memory store if not set.
# Required for production (persistence across restarts, shared state).
# REDIS_URL=redis://localhost:6379
